# メタモル セットアップガイド
**所要時間：約30〜45分**

---

## 全体の流れ

```
STEP 1  Googleスプレッドシートを作成
STEP 2  Google Apps Script を設定・デプロイ
STEP 3  GitHub にファイルをアップロード
STEP 4  GitHub Pages で公開
STEP 5  スマホのホーム画面に追加（PWA）
STEP 6  アプリ内の初期設定
```

---

## STEP 1｜Googleスプレッドシートを作成

### 1-1. 新規スプレッドシートを作成

1. ブラウザで https://sheets.google.com を開く
2. 左上の **「＋ 空白」** をクリック
3. 左上のファイル名「無題のスプレッドシート」をクリックして  
   → **「メタモル」** に変更

### 1-2. スプレッドシートIDをコピー

URLバーを見ると以下のような形式になっています：

```
https://docs.google.com/spreadsheets/d/【ここがID】/edit
```

`/d/` と `/edit` の**間の長い文字列**（例: `1aBcDeFgHiJkL...`）をコピーしてメモ帳等に貼り付けておく。

> ⚠️ このIDは後でGASコードに貼り付けます

---

## STEP 2｜Google Apps Script を設定・デプロイ

### 2-1. GASエディタを開く

1. スプレッドシートのメニューバーから  
   **「拡張機能」→「Apps Script」** をクリック
2. GASエディタが新しいタブで開く

### 2-2. コードを貼り付ける

1. エディタ左側に **「コード.gs」** が表示されている
2. 中の内容を**全て削除**（Ctrl+A → Delete）
3. ダウンロードした **`Code.gs`** の内容を**全てコピー**して貼り付ける

### 2-3. 設定値を変更する（重要）

貼り付けたコードの上部にある2箇所を変更します：

```javascript
// ↓ STEP 1-2 でコピーしたIDに変更
const SPREADSHEET_ID = 'YOUR_SPREADSHEET_ID_HERE';

// ↓ 任意のパスワードに変更（英数字推奨、例: 'mytoken2024'）
const REQUEST_TOKEN  = 'YOUR_SHARED_TOKEN_HERE';
```

> ⚠️ `REQUEST_TOKEN` はアプリとGASの**合言葉**です。  
> 自分だけが知る文字列にしてください。メモしておきましょう。

### 2-4. 保存する

- **Ctrl + S**（Mac: Cmd + S）で保存  
  または フロッピーアイコン（💾）をクリック

### 2-5. 動作テストを実行する（推奨）

1. 上部のドロップダウンから **「test_doGet」** を選択
2. **「実行」** ボタン（▶）をクリック
3. 初回は権限確認ダイアログが出る：  
   **「権限を確認」→「自分のGoogleアカウントを選択」→「詳細」→「メタモル（安全ではないページ）に移動」→「許可」**
4. 下部のログに `{"status":"ok","data":[]}` のようなJSONが出れば成功

### 2-6. Web App としてデプロイ

1. 右上の **「デプロイ」** ボタンをクリック
2. **「新しいデプロイ」** を選択
3. 以下のように設定する：

| 項目 | 設定値 |
|------|--------|
| 種類 | **ウェブアプリ** |
| 説明 | `メタモル API v1`（任意） |
| 次のユーザーとして実行 | **自分（自分のメールアドレス）** |
| アクセスできるユーザー | **全員（匿名ユーザーを含む）** |

4. **「デプロイ」** ボタンをクリック
5. 表示された **ウェブアプリURL** をコピーしてメモ帳に貼り付ける

```
例: https://script.google.com/macros/s/AKfycbxXXXXXXXX/exec
```

> ⚠️ このURLは後でアプリに設定します

---

## STEP 3｜GitHubにファイルをアップロード

### 3-1. GitHubアカウントを作成（未作成の場合）

https://github.com にアクセスして無料アカウントを作成。

### 3-2. 新しいリポジトリを作成

1. GitHubにログイン後、右上の **「＋」→「New repository」** をクリック
2. 以下のように設定：

| 項目 | 設定値 |
|------|--------|
| Repository name | `metamor`（任意の英数字） |
| Public / Private | **Public**（GitHub Pagesの無料利用に必要） |
| Add a README file | ✅ チェックを入れる |

3. **「Create repository」** をクリック

### 3-3. ファイルをアップロード

1. 作成されたリポジトリのページで **「Add file」→「Upload files」** をクリック
2. 以下のファイルをドラッグ＆ドロップ：

```
📁 アップロードするファイル一覧
├── index.html      ← メインアプリ
├── manifest.json   ← PWA設定
└── sw.js           ← Service Worker
```

3. 下部のコメント欄に `Initial upload` と入力
4. **「Commit changes」** をクリック

> 💡 `Code.gs` はGitHubには不要です（GASエディタにのみ存在）

---

## STEP 4｜GitHub Pages で公開

### 4-1. Pages を有効化

1. リポジトリのページ上部の **「Settings」** タブをクリック
2. 左サイドバーの **「Pages」** をクリック
3. **「Branch」** のドロップダウンで **「main」** を選択
4. フォルダは **「/ (root)」** のまま
5. **「Save」** をクリック

### 4-2. 公開URLを確認

数分後（最大5分）に画面上部に以下のような公開URLが表示されます：

```
https://【あなたのGitHubユーザー名】.github.io/metamor/
```

このURLにブラウザでアクセスしてアプリが表示されれば完了です。

---

## STEP 5｜スマホのホーム画面に追加（PWA）

### iPhoneの場合（Safari推奨）

1. Safari で STEP 4 の公開URLを開く
2. 画面下部の **「共有ボタン（□↑）」** をタップ
3. 下にスクロールして **「ホーム画面に追加」** をタップ
4. 名前を確認して **「追加」** をタップ

### Androidの場合（Chrome推奨）

1. Chrome で公開URLを開く
2. アドレスバー右の **「⋮」** をタップ
3. **「ホーム画面に追加」** または **「アプリをインストール」** をタップ

---

## STEP 6｜アプリ内の初期設定

ここからはアプリを開いて行う設定です。

### 6-1. 管理者モードに入る

1. アプリを開く（デモアイコンが12個表示されている）
2. グリッドの一番右下にある **「⚙️ 管理者」** アイコンをタップ
3. PINの入力画面が出る
4. 初期PIN **「3150」** を入力 → 管理者モードに入る

> ✅ 上部に緑のバー「編集モード」が表示されれば成功

### 6-2. GAS URLとトークンを設定

1. 下部タブバーの **「⚙ 設定」** をタップ
2. **「GAS Web App URL」** 欄に STEP 2-6 でコピーしたURLを貼り付け
3. **「リクエストトークン」** 欄に STEP 2-3 で設定した `REQUEST_TOKEN` を入力
4. **「💾 GAS URLを保存」** をタップ

### 6-3. GASと同期

1. 設定画面の **「↓ GASから同期」** をタップ  
   または タブバーの **「🔄 更新」** をタップ
2. 「✅ GASから同期しました」が表示されれば成功

### 6-4. PINを変更する（推奨）

初期PINの「3150」から自分だけが知るPINに変更します。

1. 設定画面の **「パスワード変更」** セクション
2. **「新しいPIN」** 欄に4桁の数字を入力
3. **「🔑 PINを変更する」** をタップ
4. 現在のPIN（3150）を入力して本人確認
5. 変更完了

> ⚠️ 変更後のPINを忘れると管理者モードに入れなくなります

### 6-5. 完了

下部タブバーの **「✓ 完了」** をタップして通常モードに戻る。

---

## アイコンの追加方法（基本操作）

1. 管理者モードに入る（⚙️アイコン → PIN）
2. 空きのコマ（点線枠）をタップ
3. 追加パネルが開く：
   - アプリ名を入力（例: `Googleマップ`）
   - URLを入力（例: `https://maps.google.com`）
   - アイコンに絵文字を入力（例: `🗺`）
   - 背景色を選択
4. **「＋ 追加する」** をタップ
5. タブバーの **「✓ 完了」** で保存

---

## アイコンの移動方法

1. 管理者モードに入る
2. 移動したいアイコンを**約0.3秒長押し**
3. 震動フィードバックが来たらドラッグ開始
4. 空きのコマ（緑に光る）にスライドして指を離す

---

## トラブルシューティング

| 症状 | 対処法 |
|------|--------|
| GAS同期でエラーが出る | GAS URLとトークンが正しいか確認。GASが「全員アクセス可」で再デプロイ |
| アイコンが表示されない | 設定→GASから同期 をタップ |
| PINを忘れた | 設定アプリ→Safari→履歴とWebサイトのデータを消去でリセット（データも消える） |
| ホーム画面のアイコンが白い | `manifest.json` と `icons/` フォルダが正しくアップロードされているか確認 |
| GASコードを変更した | 「デプロイを管理」→編集（鉛筆）→「新しいバージョン」で再デプロイが必要 |

---

## ファイル構成まとめ

```
GitHubリポジトリ（公開）
├── index.html       ← メインアプリ本体
├── manifest.json    ← PWA設定（アイコン・アプリ名など）
└── sw.js            ← オフライン対応

GASプロジェクト（非公開）
└── Code.gs          ← APIサーバー（スプレッドシートと接続）

Googleスプレッドシート（非公開）
└── リンク シート    ← アイコンデータの保存場所
```

---

*作成日：2026年5月*
